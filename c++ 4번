#include <iostream>
 
using namespace std;
 
int main() {
	int n, sum = 1;
	cout << "n의 값을 입력하세요 : ";
	cin >> n;
 
	cout << "1";
	for (int i = 2; i < n; i++) {
		if (n % i == 0) {
			sum += i;
			cout << " + " << i;
		}
	}
	cout << " = " << sum;
}
